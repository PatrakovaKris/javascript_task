—читаем бобы.
—имвол номер N строки можно получить, добавив к ней .charAt(N) ( УстрочкаФ.charAt(5) )
 Ц схожим образом с получением длины строки при помощи .length. 
¬озвращаемое значение будет строковым, состо€щим из одного символа (к примеру, УкФ).
” первого символа строки позици€ 0, что означает, что у последнего символа позици€ будет string.length Ц 1.
ƒругими словами, у строки из двух символов длина 2, а позиции еЄ символов будут 0 и 1.
Ќапишите функцию countBs, котора€ принимает строку в качестве аргумента,
 и возвращает количество символов УBФ, содержащихс€ в строке.
«атем напишите функцию countChar, котора€ работает примерно как countBs,
 только принимает второй параметр Ч символ, который мы будем искать в строке (вместо того, чтобы просто считать количество символов УBФ).
 ƒл€ этого переделайте функцию countBs.
